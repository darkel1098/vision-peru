---
const { url, title, current } = Astro.props;
---

<a
  href={url}
  class:list={[current ? "text-gray-900" : "text-gray-500 hover:text-gray-700",
  "group relative min-w-0 flex-1 overflow-hidden bg-white dark:bg-neutral-800 dark:text-gray-200 py-4 px-4 text-center text-sm font-medium hover:bg-gray-50 focus:z-10"]}
  aria-current={current ? "page" : undefined}
>
  <span>{title}</span>
  <span
    aria-hidden="true"
    class:list={[current ? "bg-gradient-to-l from-yellow-300 via-red-600 to-lime-500" : "bg-transparent",
    "absolute inset-x-0 bottom-0 h-0.5"]}></span>
</a>
